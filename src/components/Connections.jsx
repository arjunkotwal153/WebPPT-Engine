// Global connections removed — the GSAP camera movement tells the journey story.
// Scene-internal connection lines are drawn within each scene's own SVG, 
// properly clipped to their diagram containers.
const Connections = () => null;
export default Connections;
